//link to db
//var mongoose = require('mongoose');

//define the model 
/*var PhotographerSchema = new mongoose.Schema({
    photographer: String,
    logo: Image,
    type: String,
    location: String,
    price: String,
    rating: Number    
});

//give acces to other files
module.exports = mongoose.model('Photographer', PhotographerSchema);